import { Observable } from 'rxjs';
export declare const enum SOURCE {
    ESCAPE = 0,
    CLICK = 1
}
export declare enum DropdownAutoCloseType {
    Inside = "inside",
    Outside = "outside"
}
export declare function closest(element: HTMLElement, selector?: string): HTMLElement | null;
/**
 * Enables the dropdown menu to be closed on ESC and outside clicks.
 * @param document
 * @param type
 * @param close
 * @param closed$
 * @param insideElements
 * @param ignoreElements
 * @param insideSelector
 */
export declare function dropdownAutoClose(document: any, type: boolean | DropdownAutoCloseType, close: (source: SOURCE) => void, closed$: Observable<any>, insideElements: HTMLElement[], ignoreElements?: HTMLElement[], insideSelector?: string): void;
